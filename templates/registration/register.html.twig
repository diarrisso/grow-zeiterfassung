{% extends 'base.html.twig' %}

{% block body %}
    <div class="bloc-form">
    <img src="{{ asset('images/grow.svg') }}" width="400" height="100" class="logo" alt="logo-growe">
    <div class="row d-flex justify-content-center align-items-center">
        <div class="col-10 col-md-6 col-lg-3 mx-auto">
            {{ form_start(registrationForm)}}
            {{ form_widget(registrationForm) }}

            <button type="submit" class="btn btn-sm btn-md btn-lg btn-primary "> Registrieren </button>
        </div>
    </div>
    </div>
{% endblock %}
